import './style.css'
import Phaser from 'phaser'
import config from './config/config'

// Create the game instance
// The game instance uses config.js to set the game configuration
const game = new Phaser.Game(config)